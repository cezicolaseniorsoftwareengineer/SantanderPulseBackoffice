# Copy this file to .env and replace the values with your Google OAuth credentials
# The backend reads these variables at startup. You can export them in your shell or
# use the provided PowerShell script in ../scripts/set-google-oauth.ps1

GOOGLE_CLIENT_ID=your-google-oauth-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-oauth-client-secret
GOOGLE_REDIRECT_URI=http://localhost:8080/api/login/oauth2/code/google
FRONTEND_URL=http://localhost:4200
